// Melingo Configuration
// Change this URL to point to your server:
// For local development: use localhost
// For Shopify live store: use tunnel or deployed server

window.MELINGO_API_URL = 'http://localhost:8000';  // Local development

window.MELINGO_DEBUG = true;

window.MELINGO_SESSION_TIMEOUT = 30 * 60 * 1000; 

// Alternative configurations:
// window.MELINGO_API_URL = 'https://melingo-tracker.loca.lt';  // Tunnel URL
// window.MELINGO_API_URL = 'https://your-modal-app.modal.run';  // Modal deployment
// window.MELINGO_API_URL = 'https://your-app.herokuapp.com';  // Cloud deploymentfiguration
// Change this URL to point to your server:
// For local development with tunnel: use the localtunnel URL
// For production: use your deployed server URL

window.MELINGO_API_URL = 'https://bright-zebras-sell.loca.lt';  // Your current tunnel URL

window.MELINGO_DEBUG = true;

window.MELINGO_SESSION_TIMEOUT = 30 * 60 * 1000; 

// Alternative configurations:
// window.MELINGO_API_URL = 'http://localhost:8000';  // Direct local (won't work from Shopify)
// window.MELINGO_API_URL = 'https://your-modal-app.modal.run';  // Modal deployment
// window.MELINGO_API_URL = 'https://your-app.herokuapp.com';  // Heroku deployment
